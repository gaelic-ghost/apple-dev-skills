interface:
  display_name: "Apple Xcode Hybrid Orchestrator"
  short_description: "Route Apple tasks via MCP-first hybrid workflow"
  default_prompt: "Use $apple-xcode-hybrid-orchestrator to classify this Apple/Xcode task, apply MCP-first execution, and route safe fallbacks with docs and mutation guardrails."
