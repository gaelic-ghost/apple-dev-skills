interface:
  display_name: "Apple Dash Docsets"
  short_description: "Manage Dash docsets, installs, and fallback guidance"
  default_prompt: "Use $apple-dash-docsets to search Dash docsets, plan installs for missing docsets, or follow the right fallback path when Dash content is unavailable."
