interface:
  display_name: "Swift Package Workflow"
  short_description: "Compatibility surface that routes broad package work"
  default_prompt: "Use $swift-package-workflow when older references still point at that broad package workflow name, then route the request into $swift-package-build-run-workflow or $swift-package-testing-workflow while preserving the mixed-root handoff to $xcode-app-project-workflow."
