interface:
  display_name: "Apple Swift Package Bootstrap"
  short_description: "Bootstrap Swift packages with defaults and validation"
  default_prompt: "Use $apple-swift-package-bootstrap to create a new Swift package with consistent defaults, generated AGENTS guidance, and immediate validation."
