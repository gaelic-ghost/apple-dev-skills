interface:
  display_name: "Apple Swift CLI Fallback"
  short_description: "Run official Apple and Swift fallback commands safely"
  default_prompt: "Use $apple-swift-cli-fallback to execute official xcodebuild, xcrun, and swift fallback commands with concise MCP advisories and allowlist guidance."
